# Engine package — KPI computations, dimension scoring, column resolution,
# data loading and report building for the AIVault assessment app.
#
# Modules here are pragmatic implementations sized to drive the full wizard on
# real or synthetic SAP data; they can be swapped one-for-one with a richer
# Accenture v1 engine that exposes the same import surface (each module's
# public functions / dataclasses are stable and documented in-line).
